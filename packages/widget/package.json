{
  "name": "@smb/widget",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/strip-loader-export.js && node scripts/copy-static.js",
    "dev": "ts-node src/loader.ts",
    "pretest": "npm run build",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}